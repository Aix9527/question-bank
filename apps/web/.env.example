# ===== 专科复习在线题库 · Web 环境变量模板（v0.6-cloud） =====
# 复制为 .env.local（本地）或在 Vercel Project 中配置（生产）。

# 浏览器访问的 API 基地址（生产 = Vercel API Project 域名）：
# NEXT_PUBLIC_API_BASE_URL=https://question-bank-api.vercel.app
# 本地开发默认走 8000：
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# 服务端（route handlers / proxy）访问 API 的内部地址；本地与 API 同机时用 8000
API_INTERNAL_BASE=http://127.0.0.1:8000
